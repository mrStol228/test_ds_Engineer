# test_ds_Engineer

- `islands/` — count islands in a grid (BFS flood fill)
- `regression/` — predict a target from 53 anonymized features, scored on RMSE
- `mnist/` — DigitClassifier with CNN / RandomForest / random baseline behind one interface
